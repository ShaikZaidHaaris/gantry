[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gantry-policy-probe"
version = "0.1.0.dev0"
description = "A cheap fitted policy, for asking whether data carries signal at all"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = ["gantry-core", "numpy>=1.24"]

[project.optional-dependencies]
# curate_control is a TEST dependency: the probe proves real data beats
# its own shuffled control, and shuffled() lives there.
dev = ["pytest>=7.4", "gantry-curate-control"]

[project.entry-points."gantry.policy"]
probe = "gantry_policy_probe:ProbeLearner"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
