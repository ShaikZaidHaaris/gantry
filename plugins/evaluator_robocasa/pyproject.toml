[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gantry-evaluator-robocasa"
version = "0.1.0.dev0"
description = "Runs a policy over RoboCasa kitchen tasks and scene layouts"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
# The simulator is not a hard dependency. This plugin installs on a laptop, and
# its descriptor and task list can be read and planned against with nothing
# heavier than numpy present.
dependencies = ["gantry-core", "numpy>=1.24"]

[project.optional-dependencies]
sim = ["robocasa", "robosuite"]
dev = ["pytest>=7.4"]

[project.entry-points."gantry.evaluators"]
robocasa = "gantry_evaluator_robocasa:RobocasaEvaluator"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
